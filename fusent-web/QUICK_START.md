# 🚀 Быстрый старт для production сервера

## Текущая проблема
❌ Сайт падает каждый день, потому что используется `npm run dev`

## Решение - одна команда!

```bash
cd /path/to/fusent-web && ./deploy.sh
```

Или вручную:

```bash
# 1. Установить PM2 (один раз)
npm install -g pm2

# 2. Деплой
cd fusent-web
npm install
npm run build
pm2 start ecosystem.config.js
pm2 save
```

## ✅ Что это даст:

1. **Автоматический перезапуск** - если процесс упадет, PM2 автоматически перезапустит
2. **Оптимизация** - production build работает в 2-3 раза быстрее
3. **Стабильность** - нет утечек памяти как в dev-режиме
4. **Логи** - все ошибки сохраняются в `logs/`
5. **Автозапуск** - после перезагрузки сервера приложение запустится автоматически

## 📊 Управление

```bash
pm2 status              # Статус
pm2 logs fusent-web     # Логи в реальном времени
pm2 restart fusent-web  # Перезапустить
pm2 monit               # Мониторинг CPU/RAM
```

## 🔧 Конфигурация для вашего сервера

Backend API: `http://85.113.27.42:901/api/v1`
Frontend port: `3000` (проброс уже настроен на сервере)

Все настроено в файле `.env.production`

## ⚠️ ВАЖНО

**НИКОГДА не используйте `npm run dev` на production!**

Только:
- `npm run build` (собрать)
- `npm start` (запустить)
- или `pm2 start ecosystem.config.js` (лучше)

## 📝 Подробности

Смотрите [DEPLOYMENT.md](./DEPLOYMENT.md) для полной инструкции.
